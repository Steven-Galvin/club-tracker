import { SortBySexPipe } from './sort-by-sex.pipe';

describe('SortBySexPipe', () => {
  it('create an instance', () => {
    const pipe = new SortBySexPipe();
    expect(pipe).toBeTruthy();
  });
});
