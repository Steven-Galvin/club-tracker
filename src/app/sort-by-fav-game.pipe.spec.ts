import { SortByFavGamePipe } from './sort-by-fav-game.pipe';

describe('SortByFavGamePipe', () => {
  it('create an instance', () => {
    const pipe = new SortByFavGamePipe();
    expect(pipe).toBeTruthy();
  });
});
