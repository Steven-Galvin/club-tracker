import { SortByRankPipe } from './sort-by-rank.pipe';

describe('SortByRankPipe', () => {
  it('create an instance', () => {
    const pipe = new SortByRankPipe();
    expect(pipe).toBeTruthy();
  });
});
